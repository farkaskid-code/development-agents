[project]
name = "development-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "farkaskid", email = "farkaskid.inbox@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "rich>=15.0.0",
]

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[project.scripts]
project-planner = "development_agents.project_planner:main"
software-architect = "development_agents.software_architect:main"

